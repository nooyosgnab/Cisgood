#include <stdio.h>

int main() {
    int n;
    int arr[31] = {0};

    for (int i = 1; i <= 28; i++) {
        scanf("%d", &n);
        arr[n]++;
    }

    for (int j = 1; j < 31; j++) {
        if (arr[j] == 0)
            printf("%d\n", j);
    }
}
